<h1>
  Species Count -- Averaged by Year
</h1>
<br>

<%= render 'student_analyze_graph_instructions' %>

<h6>
  <% # TODO: allow download in a cross table format (by year?) %>
  <%= link_to 'CSV Download',
                species_avg_by_year_path(format: :csv) %>
</h6>

<%= render 'species_avg_display', species: @species %>
